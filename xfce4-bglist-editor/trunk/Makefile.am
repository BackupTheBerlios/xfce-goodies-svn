@SET_MAKE@

SUBDIRS = src

EXTRA_DIST = autogen.sh TODO
# config.rpath 
#spec


ACLOCAL_AMFLAGS = -I m4

DIST_SUBDIRS = src

#rpm: dist, rpmbuild -ta $(PACKAGE)-$(VERSION).tar.gz, rm -f $(PACKAGE)-$(VERSION).tar.gz

